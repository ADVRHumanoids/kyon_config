# UDP bridge, end 2. See doc/parameters.md for what each parameter does.
/**/*:
  ros__parameters:
    networkInterface: "network_bridge::UdpInterface"

    UdpInterface:
      localAddress: 127.0.0.1 #e.g. 10.8.0.2
      receivePort: 5001
      remoteAddress: 10.0.0.10 #e.g. 10.8.0.1
      sendPort: 5002

    defaultRate: 100.0

    defaultZstdLevel: 3

    useCloudini: true
    cloudiniResolution: 0.001

    useGstreamer: true
    gstreamerBitrateKbps: 4000
    gstreamerGopSize: 30
    decoderPipelineFile: "config/topic_bridge/decoders/h264_gpu.yaml"

    publishStaleData: False


    publishNamespace: "/udp2"
